{
  "name": "@packages/env",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Environment package offering basic environment functions",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome lint .",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}