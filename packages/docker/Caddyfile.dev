{
  admin off
  debug
}

:80 {
   @api path /api /api/*


  handle @api {
    header Response-Matched "api"
    reverse_proxy api:3000
  }

  # Everything else: return a 404 (so nothing ever tries to hit "web")
  handle {
    header Response-Matched "none"
    reverse_proxy web:5173
  }
}